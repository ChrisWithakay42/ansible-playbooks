---
# tasks file for helm-sealed-secrets

- name: Install Sealed Secrets Helm chart
  kubernetes.core.helm:
    name: sealed-secrets
    chart_ref: sealed-secrets/sealed-secrets
    release_namespace: kube-system
    create_namespace: yes
    wait: yes
  become: no
